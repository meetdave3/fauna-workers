{
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "name": "fauna-workers",
  "description": "A template for kick starting a Cloudflare Workers project using Fauna, the data API for modern applications.",
  "main": "build/index.mjs",
  "scripts": {
    "build": "worktop build index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Fauna Labs <https://github.com/fauna-labs/>",
  "license": "MIT-0",
  "dependencies": {
    "@fauna-labs/fauna-schema-migrate": "^2.2.1",
    "faunadb": "^4.5.4",
    "node-fetch": "^3.2.10",
    "worktop": "^0.7.3"
  },
  "devDependencies": {
    "prettier": "^1.18.2",
    "worktop.build": "^0.0.4"
  }
}
